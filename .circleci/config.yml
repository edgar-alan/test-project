version: 2.1
orbs:
  sfdx: circleci/salesforce-sfdx@2.1.1
jobs:
  authenticate:
    executor: sfdx/default
    steps:
      - sfdx/install
      - run:
          name: Check Auth List
          command: sfdx auth -h
workflows:
  workflow: 
    jobs:
      - authenticate